import React from "react";
import "@creativebulma/bulma-divider/dist/bulma-divider.css";

const Divider = (props) => {
  return (
    <div className="divider">{ props.title }</div>
  )
}

export default Divider;
